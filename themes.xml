<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- SPLASH THEME -->
    <style name="Theme.MyNotes.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_bg</item>
    </style>

    <!-- MAIN THEME -->
    <style name="Theme.MyNotes" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/purple_bg</item>
    </style>

</resources>
